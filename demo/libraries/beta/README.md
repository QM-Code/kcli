# Beta Demo Library

Inline-mode demo library for `kcli`.

Exports:
- `kcli::demo::beta::ProcessCLI(argc, argv, root)`
- `kcli::demo::beta::EmitDemoOutput()`

`ProcessCLI` consumes `--<root>` and `--<root>-*` options.
