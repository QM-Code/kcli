# Alpha Demo Library

Inline-mode demo library for `kcli`.

Exports:
- `kcli::demo::alpha::ProcessCLI(argc, argv, root)`
- `kcli::demo::alpha::EmitDemoOutput()`

`ProcessCLI` consumes `--<root>` and `--<root>-*` options.
