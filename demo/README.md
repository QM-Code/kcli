# KCLI Demos

This directory mirrors the shape of `ktrace/demo/`:
- `libraries/alpha`
- `libraries/beta`
- `libraries/gamma`
- `executable`

The libraries demonstrate inline-mode parsing (`Initialize(argc, argv, root)`).
The executable is a compile/link/integration check that wires those libraries
into one app and runs a minimal end-user parser pass.
